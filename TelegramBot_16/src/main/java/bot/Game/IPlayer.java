package bot.Game;

/**
 * interface that implements the player method
 */
public interface IPlayer {
    /**
     * this method deals with the questions that a player gets
     * the method also deals with the correct answer expected
     * this method does not return anything
     */
    void Player();
}